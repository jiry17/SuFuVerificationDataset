(
  (data Bool (True False))
  (:: True Bool)
  (:: False Bool)
  (data Nat (Zero Succ))
  (:: Zero Nat)
  (:: Succ (-> (Nat) Nat))
  (data Tuple3 (pvar0 pvar1 pvar2) (MakeTuple3))
  (:: MakeTuple3 (-> (pvar0 pvar1 pvar2) (Tuple3 pvar0 pvar1 pvar2)))
  (data Tree (Leaf Node))
  (:: Leaf (-> (Nat) Tree))
  (:: Node (-> ((Tuple3 Nat Tree Tree)) Tree))
  (data Tuple2 (pvar0 pvar1) (MakeTuple2))
  (:: MakeTuple2 (-> (pvar0 pvar1) (Tuple2 pvar0 pvar1)))
  (:: nateq (-> (Nat Nat) Bool))
  (let nateq (Zero Zero) True)
  (let nateq (Zero (Succ ?x)) False)
  (let nateq ((Succ ?x) Zero) False)
  (let nateq ((Succ ?x) (Succ ?y)) (nateq ?x ?y))
  (:: and (-> (Bool Bool) Bool))
  (let and (False ?x) False)
  (let and (?x False) False)
  (let and (True True) True)
  (:: tf1 (-> ((Tuple2 Tree Tree)) Bool))
  (let tf1 ((MakeTuple2 (Leaf ?tv5) (Leaf ?tv6))) (nateq ?tv5 ?tv6))
  (let tf1 ((MakeTuple2 (Node (MakeTuple3 ?tv7 ?tv8 ?tv9)) (Node (MakeTuple3 ?tv10 ?tv11 ?tv12)))) (and (nateq ?tv7 ?tv10) (and (tf0 ?tv9 ?tv11) (tf0 ?tv8 ?tv9))))
  (let tf1 ((MakeTuple2 (Node ?tv13) (Leaf ?tv14))) False)
  (let tf1 ((MakeTuple2 (Leaf ?tv15) (Node ?tv16))) False)
  (:: tf0 (-> (Tree Tree) Bool))
  (let tf0 (?tv2 ?tv3) (tf1 (MakeTuple2 ?tv2 ?tv3)))
  (:: issympair (-> (Tree Tree) Bool))
  (let issympair (?tv0 ?tv1) (tf0 ?tv0 ?tv1))
  (:: tf3 (-> (Tree) Bool))
  (let tf3 ((Leaf ?tv20)) True)
  (let tf3 ((Node (MakeTuple3 ?tv21 ?tv22 ?tv23))) (and (issympair ?tv22 ?tv23) (and (tf2 ?tv22) (tf2 ?tv23))))
  (:: tf2 (-> (Tree) Bool))
  (let tf2 (?tv18) (tf3 ?tv18))
  (:: issym (-> (Tree) Bool))
  (let issym (?tv17) (tf2 ?tv17))
  (:: plus (-> (Nat Nat) Nat))
  (let plus (Zero ?x) ?x)
  (let plus ((Succ ?x) ?y) (Succ (plus ?x ?y)))
  (:: tf5 (-> (Tree) Nat))
  (let tf5 ((Leaf ?tv27)) ?tv27)
  (let tf5 ((Node (MakeTuple3 ?tv28 ?tv29 ?tv30))) (plus ?tv28 (plus (tf4 ?tv29) (tf4 ?tv30))))
  (:: tf4 (-> (Tree) Nat))
  (let tf4 (?tv25) (tf5 ?tv25))
  (:: spec (-> (Tree) Nat))
  (let spec (?tv24) (tf4 ?tv24))
  (:: tf7 (-> (Tree) Tree))
  (let tf7 ((Leaf ?tv34)) (Leaf ?tv34))
  (let tf7 ((Node (MakeTuple3 ?tv35 ?tv36 ?tv37))) (Node (MakeTuple3 ?tv35 (tf6 ?tv36) ?tv37)))
  (:: tf6 (-> (Tree) Tree))
  (let tf6 (?tv32) (tf7 ?tv32))
  (:: target (-> (Tree) Tree))
  (let target (?tv31) (tf6 ?tv31))
  (:: ite (-> (Bool pvar0) pvar0))
  (let ite (True ?x) ?x)
  (let ite (False ?x) Zero)
  (:: main (-> (Tree) Nat))
  (let main (?tv38) (ite (issym ?tv38) (spec (target ?tv38))))
  (:: times (-> (Nat Nat) Nat))
  (let times (Zero ?x) Zero)
  (let times ((Succ ?x) ?y) (plus (times ?x ?y) ?y))
  (:: tf9 (-> (Tree) Nat))
  (let tf9 ((Leaf ?tv42)) ?tv42)
  (let tf9 ((Node (MakeTuple3 ?tv43 ?tv44 ?tv45))) (plus ?tv43 (times (Succ (Succ Zero)) (tf8 ?tv44))))
  (:: tf8 (-> (Tree) Nat))
  (let tf8 (?tv40) (tf9 ?tv40))
  (:: targetNew (-> (Tree) Nat))
  (let targetNew (?tv39) (tf8 ?tv39))
  (:: mainNew (-> (Tree) Nat))
  (let mainNew (?tv46) (ite (issym ?tv46) (targetNew ?tv46)))
  (=== optimize (inp0) (Tree) (main inp0) (mainNew inp0))
)
